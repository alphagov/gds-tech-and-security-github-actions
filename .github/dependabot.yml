---
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    cooldown:
      default: 7  # Wait 7 days after a version is released before opening PR
    ignore:
      - dependency-name: "aquasecurity/setup-trivy" # temporary ignore
